# AGENTS.md

Local rules for `notebooks/`.

- Use notebooks for exploration only.
- Call into `src/kryptonite/` and `scripts/` for reusable logic.
- Promote any reusable workflow back into code once it stops being exploratory.
- Do not keep production-only logic or data contracts exclusively in notebooks.
