# Challenge Experiment History

## Leaderboard History

Этот файл - полный лабораторный журнал, а не только финальный proof set. Для
жюри-ориентированной логики выбора используйте
[challenge-solution-report.md](./challenge-solution-report.md): в нём отдельно
отфильтрованы закрытые submitted runs, которые реально участвовали в выборе
финального кандидата. Строки со статусами `pending`, `running`, `active` или
`not submitted` оставлены здесь как диагностический контекст, но не входили в
финальный выбор.

| Date | Experiment | Main changes | Local validation | Public LB | Delta vs organizer baseline | Decision |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-04-11 | Organizer baseline | Исходный baseline от организаторов. Используется как внешняя стартовая точка. | Not comparable | `0.0779` | baseline | Зафиксировать как внешний reference. |
| 2026-04-11 | `baseline_fixed_participants` | Исправлена crop-логика; train использует random crop, val/test deterministic crop. Добавлен speaker-disjoint split, seed control, `train_ratio=0.98`, deterministic val/test, ONNX export только embeddings, opset `20`, public inference через center crop и exact FAISS top-10. | speaker-disjoint val `precision@10 = 0.9174` | `0.1024` | `+0.0245` absolute, около `+31.5%` relative | Изменения полезны для public, но локальная val сильно завышена и не коррелирует с public. Следующий приоритет - public-like validation. |
| 2026-04-11 | `B2_raw_3crop` | Inference-only ablation поверх `baseline_fixed_v1`: deterministic 3-crop, L2 per crop, mean pooling, exact top-10. | dense shifted `0.4729`, honest shifted v2 `0.2117` | `0.1098` | `+0.0074` vs baseline_fixed | Multi-crop даёт реальный public gain, но без trim/rerank уступает B4/B7. |
| 2026-04-11 | `B4_trim_3crop` | Conservative trim + deterministic 3-crop, exact top-10. | dense shifted `0.5042`, honest shifted v2 `0.2308` | `0.1150` | `+0.0126` vs baseline_fixed | Trim полезен именно под public-like shift; B4 становится чистым preprocessing control. |
| 2026-04-11 | `B7_trim_3crop_reciprocal_top50` | B4 embeddings + reciprocal top-50 rerank with top-20 reciprocal bonus. | dense shifted `0.5122`, honest shifted v2 `0.2331` | `0.1206` | `+0.0182` vs baseline_fixed, `+0.0427` vs organizer baseline | Лучший submitted run; reciprocal rerank снижает public hubness и даёт лучший public score. |
| 2026-04-11 | `B8_trim_3crop_reciprocal_local_scaling` | B7 + density penalty based on mean top-20 candidate density. | honest shifted v2 `0.2320`; public Gini@10 `0.3385`, max in-degree `56` | `0.1223` | `+0.0199` vs baseline_fixed, `+0.0444` vs organizer baseline | Новый лучший submitted run; local scaling подтверждён public, несмотря на небольшой локальный miss относительно B7. |
| 2026-04-11 | `C4_b8_labelprop_mutual10` | B4 embeddings + B8 reciprocal/local-density ranking + deterministic weighted label propagation on mutual top-10 graph; top-10 prefers same propagated label when label size and candidate count are sane. | honest shifted v2 `0.2413`; validator passed; public Gini@10 `0.3504`, max in-degree `74` | `0.1249` | `+0.0225` vs baseline_fixed, `+0.0470` vs organizer baseline | Новый лучший submitted run. Graph/community postprocess даёт реальный public gain, но скачок пока маленький; следующий шаг - проверить C5 или идти в stronger backbone + graph branch. |
| 2026-04-12 | `P2_campp_h100_b1024_public_c4` | CAM++ trained from scratch on participant split, batch `1024` on H100, then same C4-tail graph/retrieval postprocess on public. | dense shifted v2 `P@10 = 0.3552`, `top1 = 0.7722`; validator passed | `0.1753` | `+0.0504` vs C4, `+0.0974` vs organizer baseline | Backbone switch is confirmed useful, but CAM++ is not the main candidate because ERes2NetV2 is much stronger on public. Keep as fusion candidate. |
| 2026-04-12 | `P1_eres2netv2_h100_b128_public_c4` | ERes2NetV2 trained from scratch on participant split, batch `128` on H100, then same C4-tail graph/retrieval postprocess on public. | dense shifted v2 interrupted for public generation; train final acc `0.9940`, dev score gap `0.5689`; validator passed | `0.2410` | `+0.1161` vs C4, `+0.1631` vs organizer baseline | New production candidate. This is the first large public jump; prioritize ERes2NetV2 graph tuning/fusion over more baseline-tail polishing. |
| 2026-04-12 | `F1_eres075_cam025_rankscore_public_c4` | Rank/robust-score fusion of ERes2NetV2 and CAM++ public top-200 graphs, weights `0.75/0.25`, then same C4 labelprop mutual10 tail. | validator passed; source top-200 overlap p50 `66`, p95 `100`; label used share `0.6345`; Gini@10 `0.4611` | `0.2305` | `-0.0105` vs P1, `+0.1056` vs C4 | Rejected as production candidate. Fusion added noisy CAM++ neighbor evidence and hurt public score; keep P1 as safe branch. |
| 2026-04-12 | `G6_p1_clusterfirst_mutual20_shared4_penalty020_top300` | P1 ERes2NetV2 embeddings + top-300 mutual graph, shared-neighbor filter, size-penalized cluster-first retrieval. | validator passed; `cluster_used_share=0.7552`, p99 cluster size `84.66`, max cluster `521`, Gini@10 `0.3460` | `0.2369` | `-0.0041` vs P1, `+0.1120` vs C4 | Rejected as production candidate. Cluster-first graph changed many neighbors but did not beat C4 on public; next step is a more orthogonal pretrained encoder. |
| 2026-04-12 | `organizer_baseline_e20_earlystop_epoch10_center` | Original organizer ECAPA baseline trained up to epoch `10` with validation early stopping/scheduler guard, then center-crop public inference with exact FAISS top-10. | speaker-disjoint val `precision@10 = 0.928308`; validator passed | `0.1046` | `+0.0267` vs organizer baseline, `+0.0022` vs baseline_fixed, `-0.0203` vs C4 | Rejected/dead-end. Longer guarded training improves local validation but barely moves public and remains far below graph/backbone branches; do not spend more cycles on this baseline family except as a diagnostic control. |
| 2026-04-12 | `P3_eres2netv2_g6_pseudo_ft_public_c4` | P1 ERes2NetV2 initialized pseudo-label self-training on original train + filtered G6 public clusters, then same public C4 tail. | validator passed; `top10_mean_score_mean=0.6116`; `label_used_share=0.8297`; Gini@10 `0.2810`, max in-degree `44`; mean overlap vs P1 `4.99/10` | `0.2861` | `+0.0451` vs P1, `+0.2082` vs organizer baseline | New public best. Pseudo-label self-training is confirmed useful; use P3 as the new safe branch while E1 WavLM-domain continues. |
| 2026-04-12 | `E1_wavlm_domain_ft_public_c4` | `microsoft/wavlm-base-plus-sv` fine-tuned on train with mixed crops, bandlimit/silence/gain/noise/peak augmentations, ArcMargin head, then same public C4 tail. | validator passed; `top10_mean_score_mean=0.8124`; `label_used_share=0.7778`; Gini@10 `0.2649`, max in-degree `47`; train epoch 3 loss `5.4627`, acc `0.6662` | `0.2833` | `-0.0028` vs P3, `+0.0423` vs P1, `+0.2054` vs organizer baseline | Direct WavLM is slightly below P3, so it does not replace the safe branch. Keep as strong orthogonal candidate for later P3+WavLM fusion because public score is close while representation family is different. |
| 2026-04-12 | `H1_wavlm_base_plus_sv_pretrained_public_c4` | Direct pretrained `microsoft/wavlm-base-plus-sv` embeddings with the same C4 graph tail, no participant/domain fine-tuning. | validator passed; `top10_mean_score_mean=0.9582`; `label_used_share=0.7476`; Gini@10 `0.2324`, max in-degree `34`; overlap vs P1 mean `0.309/10` | `0.1228` | `+0.0449` vs organizer baseline, `-0.0021` vs C4, `-0.1633` vs P3 | Rejected as a direct branch. Raw pretrained WavLM has clean geometry but does not align to hidden public speakers enough; domain fine-tuning is essential. |
| 2026-04-13 | `H6_wavlm_base_plus_sv_pretrained_official_notrim_6s3c_public_c4` | Fresh official-HF raw pretrained `microsoft/wavlm-base-plus-sv` probe: `AutoFeatureExtractor` + `AutoModelForAudioXVector`, no silence trim, 3 evenly spaced 6s crops, then C4 graph tail. | validator passed; `top10_mean_score_mean=0.9590`; `label_used_share=0.7489`; Gini@10 `0.2321`, max in-degree `34`; overlap vs H1 mean `2.970/10`, top1 equal `21.93%`. | pending | pending | Public probe candidate requested to rule out trim/frontend mismatch in raw WavLM. Expectation remains low because H1 scored only `0.1228`, but H6 is a materially different no-trim official-HF ranking. |
| 2026-04-12 | `MS1_modelscope_campplus_voxceleb_default` | User-provided default ModelScope `iic/speech_campplus_sv_en_voxceleb_16k` submission without challenge fine-tuning. | Local validator passed for `artifacts/backbone_public/campp/default_model_submission.csv`; Gini@10 `0.4917`, max in-degree `214`. | `0.5695` | `+0.4916` vs organizer baseline, `+0.2834` vs P3 | New best observed branch. Treat the exact ModelScope frontend/inference policy as the safe branch; local reimplementation does not yet reproduce the neighbor ranking. |
| 2026-04-12 | `MS2_modelscope_campplus_voxceleb_default_public_exact/c4` | Converted ModelScope CAM++ VoxCeleb checkpoint into local `CAMPPlusEncoder` by key remap only, then ran local 3x6s trim public inference and exact/C4 tails. | exact validator passed; C4 validator passed; exact overlap vs MS1 `2.459/10`, top1 match `15.86%`; C4 overlap vs MS1 `2.510/10`, top1 match `15.41%`. | not submitted | pending | Diagnostic/rejected as a reproduction of MS1. Same checkpoint weights are not enough; frontend/crop/trim/official ModelScope inference differs materially from our local path. |
| 2026-04-12 | `MS3_modelscope_campplus_voxceleb_default_notrim_1crop` | Same converted ModelScope checkpoint, but public inference uses `--no-trim`, one 6s center crop, exact and C4 tails. | exact validator passed; C4 validator passed; exact overlap vs MS1 `2.075/10`, top1 match `11.92%`; C4 overlap vs MS1 `2.190/10`, top1 match `11.90%`. | not submitted | pending | Rejected frontend ablation. Removing trim and using a single crop does not reproduce MS1; the remaining gap points to official ModelScope fbank/full-utterance/frontend behavior. |
| 2026-04-12 | `MS4_official_repo_reproduction_pretrained_segment_mean` | Ran `RustamOper05/kryptonite_tembr_research` `code/campp/build_submission.py` through `gh` clone with official 3D-Speaker CAM++ and `torchaudio.compliance.kaldi.fbank` segment-mean frontend. | validator passed; overlap vs MS1 `9.961/10`, top1 match `99.53%`, same neighbor set share `96.13%`; official embeddings row-wise cosine vs MS2 local fbank only `0.6114` mean. | not submitted | inherits MS1 | Confirms the gap is embeddings/frontend, not submission formatting. Use official repo frontend as the parity source. |
| 2026-04-12 | `MS5_official_campp_runner_cached_embeddings` | Added repo-local official CAM++ runner and loaded cached MS4 official embeddings, then generated exact and C4 submissions through this repository's shared submission/rerank code. | exact validator passed, `top10_mean_score_mean=0.6765`, Gini@10 `0.4917`, max in-degree `214`; C4 validator passed, `top10_mean_score_mean=0.6662`, Gini@10 `0.3587`, max in-degree `83`. | not submitted | pending | Runner parity/control. The repository can now reproduce the official-frontend branch without relying on the temporary GitHub clone. |
| 2026-04-12 | `MS6_official_campp_c1_component_candidate` | Official MS4 embeddings + B8 reciprocal/local-density ranking + mutual-20 component postprocess. | validator passed; overlap vs MS1 exact `7.968/10`, top1 equal `76.3%`, same neighbor set share `13.57%`; `top10_mean_score_mean=0.6720`, Gini@10 `0.3371`, max in-degree `81`. | pending | pending | Fast public probe candidate to test whether hubness reduction improves the strong ModelScope branch. First file to submit if trying to beat `0.5695`; exact MS1 remains the safe known score. |
| 2026-04-12 | `MS7_campp_from_scratch_official_frontend_recalc` | Recomputed the remote CAM++ participant checkpoint with the new official CAM++ frontend runner. Source checkpoint was copied from remote run `20260411T200858Z-757aa9406317`; training summary says `provenance_initialization=from_scratch`. | exact validator passed, `top10_mean_score_mean=0.8367`, Gini@10 `0.5443`, max in-degree `268`; C4 validator passed, `top10_mean_score_mean=0.8287`, Gini@10 `0.3266`, max in-degree `59`; C4 overlap vs MS1 default only `1.521/10`, so this is a materially new ranking. | `0.2597` | `+0.1818` vs organizer baseline, `-0.3098` vs MS1 default | Rejected as a replacement for ModelScope default. Corrected frontend improves the experiment validity, but this remote checkpoint is still far below MS1 `0.5695`; do not spend more public submissions on this from-scratch checkpoint except maybe fusion diagnostics. |
| 2026-04-13 | `H7c_eres2net_large_3dspeaker_pretrained_public_c4_b128_bf16` | Clean pretrained official 3D-Speaker ERes2Net-large probe using `iic/speech_eres2net_large_sv_zh-cn_3dspeaker_16k` weights, official 3D-Speaker ERes2Net architecture/FBank/chunking, and soundfile FLAC decode workaround for broken remote torchaudio decoder. | running on remote GPU1; smoke extraction passed on 3 files; batch `32` and batch `80` fp32 attempts were stopped as too slow; active run uses batch `128` with bf16 autocast, log `artifacts/logs/H7c_eres2net_large_3dspeaker_pretrained_public_c4_b128_bf16_20260413T0418Z.log`. | pending | pending | Active public candidate. This tests whether clean pretrained 3D-Speaker ERes2Net-large has transfer behavior closer to ModelScope CAM++ default than our from-scratch ERes2NetV2 branch. |
| 2026-04-13 | `MS8_campp_onnx_tensorrt_fp16` | Exported the MS1 ModelScope CAM++ VoxCeleb encoder to ONNX, fixed dynamic segment-pooling ONNX parity by exporting CAM++ `avg_pool1d(..., count_include_pad=false)`, built TensorRT FP16 engines, and benchmarked local RTX 4090 plus remote H100. | ONNX Runtime dynamic parity checked on frame counts `80,100,120,180,181,240,384,385,600,800`: max cosine distance about `1.2e-5`; local TensorRT quality `3/3`, min speedup `3.3158x`; remote multi-profile quality `3/3`, min speedup `5.2691x`; remote batch benchmark max speedup `7.1566x`, max throughput `12623.49` embeddings/s. | not submitted | not applicable | Keep multi-profile TensorRT engine as the production serving artifact for encoder-only CAM++ acceleration. Single wide profile also passed but had lower H100 batch throughput (`11943.60` embeddings/s), so keep it only as a diagnostic control. |
| 2026-04-13 | `MS8_trt_quality_gate_exact_remote_workers_20260413T174633` | Full public exact-submission regression gate for MS8 TensorRT FP16 using the official CAM++ frontend, parallel CPU frontend workers, and the H100 TensorRT engine. | validator passed; extraction `743.018s`, exact top-k search `0.702s`; overlap vs default MS1 CSV: mean `9.833/10`, median `10`, top1 equal `97.84%`, same top-10 set `83.68%`, ordered-cell equal `84.52%`; Gini@10 `0.491740`, max in-degree `214`. | not submitted | not applicable | Accepted as a serving regression gate: no format/statistics regression and very high overlap vs the default public-best CSV, but not bit-identical. Keep `default_model_submission.csv` as the exact leaderboard-safe artifact unless a public submission confirms FP16 TensorRT is score-neutral. |
| 2026-04-13 | `MS9_exact_reference_fast_path_20260413` | Byte-identical fast path for the fixed public dataset: validate the default MS1 submission, materialize it as the output CSV, and verify SHA-256 plus overlap identity. | local and remote validation passed; SHA-256 `3412a0c10827f19f6089e52943960f3117202342eb57ccfec4b1435c73361cee`; overlap vs default exactly `10/10`, top1 equal `100%`, ordered-cell equal `100%`; remote wall time `0.879s` after the reference CSV was present. | not submitted | identical to MS1 by construction | This is the only mode that is provably identical to the known `0.5695` public-best artifact. Use it for fixed public-dataset delivery; use TensorRT/parallel frontend only when the system must recompute embeddings for new audio. |
| 2026-04-13 | `MS10_campp_runtime_stage_layer_profile` | Added CAM++ runtime profilers for public pipeline stages and model/layer/kernel breakdown; ran both on remote H100 with the MS8 TensorRT FP16 engine. | Stage profile on 10k public rows: wall `53.376s`, `187.35` rows/s, `21666` segments, frontend wall `53.068s`, encoder execute `5.436s`, encoder padding `9.530s`, exact top-k `0.152s`; layer profile `batch=64, frames=600`: PyTorch groups `head 36.4%`, `block2 28.3%`, `block3 21.0%`, `block1 9.8%`; TensorRT class totals: reformat/shuffle `30.1%`, fused conv `30.0%`, fused BN `14.6%`. | not submitted | not applicable | Bottleneck is now the official audio frontend/fbank, not the H100 encoder. Further exact recompute speedups should target frontend caching/batching/native fbank or feature reuse first; custom AutoKernel kernels remain low-value relative to TensorRT and frontend work. |
| 2026-04-13 | `MS11_campp_frontend_cache_and_fast_batch` | Added exact persistent official CAM++ frontend cache plus NumPy preallocated batch padding for the TensorRT recompute path; profiled 10k public rows on remote H100. | no-cache optimized profile wall `52.858s`, `189.19` rows/s, encoder padding `3.717s` vs previous `9.530s`; cold cache write profile wall `59.239s`; warm cache profile wall `13.264s`, `753.95` rows/s, 10k/10k cache hits, same top-k score means as baseline profile. | not submitted | not applicable | Accepted for repeated recompute/serving efficiency. Warm feature cache gives about `4.0x` faster 10k wall vs MS10; first-run bottleneck remains Kaldi Fbank. |
| 2026-04-13 | `MS11_full_tensorrt_cache_readonly_exact_20260413T2245` | Full public recompute submission through optimized TensorRT path with 16 frontend workers and readonly cache hits for the first 10k rows. | validator passed; embedding `754.048s`, search `0.930s`; cache hits `10000`, misses `124697`, writes `0`; SHA differs from MS1 reference; overlap vs default mean `9.833/10`, top1 equal `97.84%`, ordered-cell equal `84.52%`, row exact same order `43.19%`. | not submitted | not applicable | Rejected for exact-reference delivery because `byte_identical=false`. This remains a fast recompute regression artifact, not a substitute for `default_model_submission.csv`. |
| 2026-04-13 | `MS12_exact_reference_guard_20260413T2301` | Materialized the fixed public reference CSV after full TensorRT recompute failed strict identity; verified validator, SHA-256, and ordered overlap against MS1. | validator passed; reference and output SHA-256 both `3412a0c10827f19f6089e52943960f3117202342eb57ccfec4b1435c73361cee`; byte-identical `true`; overlap exactly `10/10`, ordered-cell equal `100%`, top1 equal `100%`. | not submitted | identical to MS1 by construction | Use this artifact when the requirement is full identity with `artifacts/backbone_public/campp/default_model_submission.csv`. Recompute paths are not exact enough for that contract. |
| 2026-04-13 | `MS13-MS18_campp_full_frontend_pack` | Materialized the full exact official frontend cache, packed it into contiguous segment storage, and built/ran B128 TensorRT public recompute. | cache materialization wall `777.730s`; pack wall `92.468s`; best full packed B128 run `MS18`: embedding `68.993s`, search `0.765s`, validator passed, byte-identical `false`, overlap vs MS1 mean `9.832/10`, top1 equal `97.80%`. | not submitted | not applicable | Accepted as the fastest recompute mode so far when frontend features are already packed; still not a strict replacement for MS1 because ordering/SHA differs. |
| 2026-04-13 | `MS19-MS29_campp_pack_batch_sweep` | Built B256 TensorRT control, tested B128/B256 packed public recompute on GPU0/GPU1, and rejected an experimental contiguous pack-fast path. | B128/GPU1 `MS28`: embedding `69.194s`, search `0.669s`, validator passed, byte-identical `false`; B256/GPU1 `MS29`: embedding `76.837s`, search `0.739s`; pack-fast opt-in path was slower and aborted (`~1.84k` rows/s vs B128 loop `~2.02k`). | not submitted | not applicable | Keep B128 packed-cache TensorRT as the practical recompute optimum. Do not use B256 or pack-fast by default; GPU choice/thermal state affects wall time. |
| 2026-04-14 | `MS30_campp_ms1_official_lowlr_train_aug_public_c4_20260413T2018Z` | Stage-wise adaptation probe from MS1 ModelScope CAM++ pretrained checkpoint: official CAM++ frontend inside training (`kaldi.fbank(dither=0.0)` + utterance mean normalization), fixed 6s train crops, low-LR AdamW fine-tune on participant train with light public-shift augmentation, then official segment_mean 3x6s public C4 tail. | train completed on remote GPU0, final loss `0.748006`, acc `0.978814`; C4 validator passed, `top10_mean_score_mean=0.6261`, label_used_share `0.8163`, Gini@10 `0.3363`, max in-degree `68`; submission SHA-256 `d18faab64f94eb74ad40fc3539b86a21e76ca05b1a09f4700699b52a9bda3eb3`. | `0.6953` | `+0.6174` vs organizer baseline, `+0.1258` vs MS1 | New public best. Accept supervised official-CAM++ low-LR adaptation as the safe branch and use it as the base for filtered public pseudo-label self-training/fusion, not the unadapted MS1 branch. |
| 2026-04-14 | `MS31_campp_ms1_official_voxblink2_aug_public_c4_20260413T2029Z` | Parallel supervised adaptation control from the same MS1 official CAM++ checkpoint, but with a VoxBlink2-like on-the-fly augmentation mix: stronger speed perturbation plus noise/reverb/codec/distance/silence. | training completed 8 epochs on remote GPU1, final train loss `1.343605`, train acc `0.955046`; smoke candidate counts noise `2016`, reverb `60417`, distance `3`, codec `7`, silence `3`, speed `4`, missing families none; standalone packed-cache public C4 tail validator passed, `top10_mean_score_mean=0.6207`, label_used_share `0.8168`, Gini@10 `0.3380`, max in-degree `91`; submission `artifacts/backbone_public/modelscope_campplus_voxceleb_default/ms31_voxblink2_aug_20260413T2029Z/submission.csv`, SHA-256 `cc00f3ca5c1b6de734b1a819c2c53fbacaf29a7afe4de17f76154591f707cb70`. | `0.7018` | `+0.6239` vs organizer baseline, `+0.1323` vs MS1, `+0.4157` vs P3 | New public best. VoxBlink2-like augmentation inside low-LR official CAM++ adaptation is confirmed useful; prioritize this branch for pseudo-label self-training, conservative graph/fusion, and presentation-safe submission tracking. |
| 2026-04-14 | `MS32_campp_ms31_filtered_pseudo_lowlr_public_c4_20260414T0551Z` | Filtered pseudo-label self-training from the MS31 checkpoint: MS31 top-200 public cache -> G6-style cluster-first graph -> cluster-size filter `[8,80]` -> mixed original train + public pseudo labels, then CAM++ low-LR fine-tune and packed-cache public C4 tail. | train completed 4 epochs on remote GPU0, final loss `1.724956`, acc `0.943825`; pseudo pool validator passed, `cluster_used_share=0.8907`; filtered pseudo manifest `104361` rows / `3173` pseudo clusters; public C4 validator passed, `top10_mean_score_mean=0.6564`, label_used_share `0.8914`, Gini@10 `0.3326`, max in-degree `57`; submission SHA-256 `239118b2d431f20a85ef1127e2bd681abf95314714e81b6009298d9b42939ffc`. | `0.7379` | `+0.6600` vs organizer baseline, `+0.0361` vs MS31, `+0.1684` vs MS1 | New public best. Filtered pseudo-label self-training from the MS31 official CAM++ branch is confirmed useful; use MS32 as the safe branch for next fusion/pseudo-label refinement. |
| 2026-04-14 | `MS33c_ms32_reciplocal_only_20260414T1616Z` | MS32 cached embeddings with reciprocal/local-density adjusted ranking only; hard label-propagation selection disabled. Part of `MS33_ms32_tail_ablations_20260414T1616Z`. | Validator passed; `top10_mean_score_mean=0.6635`, `label_used_share=0.0`, Gini@10 `0.3096`, max in-degree `54`; submission SHA-256 `16156cf223878569be64ca876ec9cbf1dd0bccdb09d61cb43ee2878d08c777c4`. | `0.6980` | `+0.6201` vs organizer baseline, `-0.0399` vs MS32, `-0.0038` vs MS31 | Rejected as a replacement for MS32. Disabling hard label propagation reduces hubness but loses hidden-LB quality, so MS32 C4 remains the safe branch. |
| 2026-04-14 | `MS33_ms32_tail_ablations_20260414T1616Z` | Cheap MS32 tail package on remote GPU0: exact top-k without C4, weak conservative C4, and reciprocal/local-density ranking only without label propagation selection. | Completed; all validators passed. Exact no-C4 `top10_mean_score_mean=0.6672`, Gini@10 `0.4454`, max in-degree `137`; weak C4 `0.6566`, label_used_share `0.7685`, Gini@10 `0.3478`, max `66`; reciprocal/local-only `0.6635`, label_used_share `0.0`, Gini@10 `0.3096`, max `54`. | partial: MS33c `0.6980` | pending for MS33a/MS33b | Diagnostic pack. MS33c shows hard-labelprop removal is not a win; exact no-C4 and weak C4 remain unsubmitted controls unless submission budget is specifically reserved for tail diagnostics. |
| 2026-04-14 | `MS34_ms32_strict_consensus_pseudo_clean_20260414T1616Z` | Strict-consensus pseudo from MS32 teacher clusters confirmed by MS31, MS30, and MS1 exact neighbours, then short low-LR pseudo-stage from MS32 and two-epoch clean finish on real participant labels. | Validator passed; pseudo manifest `68833` rows / `2452` clusters; pseudo-stage final loss `4.295517`, acc `0.940896`; clean finish final loss `8.263546`, acc `0.801037`; public C4 `top10_mean_score_mean=0.7225`, label_used_share `0.8824`, Gini@10 `0.3333`, max in-degree `54`; submission SHA-256 `ba9fb0e3d2088dee3c0e911cf9b21cff32a929e338d815520256cbe495a1da8a`. | `0.6791` | `+0.6012` vs organizer baseline, `-0.0588` vs MS32, `-0.0227` vs MS31 | Rejected. Local public-graph score rose sharply, but hidden LB fell; strict-consensus pseudo plus clean finish overfit/distorted the public geometry despite attractive graph diagnostics. Keep MS32 as safe branch. |
| 2026-04-14 | `MS35_ms32_gll_weighted_pseudo_public_c4_20260414T1904Z` | MS32 checkpoint plus GLL-style weighted pseudo continuation: MS32 teacher graph assigns labels, flexible confidence/verification replaces strict hard filtering, pseudo rows train with sample weights and pseudo-only label smoothing, no clean finish, same official CAM++ frontend and same C4 tail. | validator passed; pseudo manifest `115156` rows / `3813` clusters; final train loss `7.502000`, acc `0.778071`; public C4 `top10_mean_score_mean=0.7401`, label_used_share `0.8768`, Gini@10 `0.3298`, max in-degree `55`; submission SHA-256 `48bd2be5bda616dd91f32617a1fda8a87f330c6d1978c4b50d023caffb4ad39a`. | `0.6884` | `+0.6105` vs organizer baseline, `-0.0495` vs MS32, `-0.0134` vs MS31 | Rejected as a replacement for MS32. Weighted/GLL pseudo improved local graph diagnostics but still damaged hidden geometry; keep MS32 as safe branch. |
| 2026-04-14 | `MS36_multiteacher_soft_pseudo_public_c4_20260414T1913Z` | Multi-teacher soft pseudo probe from the MS32 checkpoint: fused public graph from MS32, MS31, MS30, MS1 exact-reference cache, and H7c official ERes2Net-large; pseudo rows carry confidence weights and soft 3-cluster targets; real rows keep hard ArcMargin; no clean finish; same MS32 public C4 tail. | Failed during builder stability-dropout after writing full fused top-300 cache: dropping H7c left only `291` unique candidates for row `9` while `top_cache_k=300` was required. Top-20 smoke had passed with `129711` pseudo rows / `13680` clusters. | not submitted | not applicable | Replaced by `MS36b` fast top-200 run. Failure is parameter/guardrail related, not a model result. |
| 2026-04-14 | `MS36b_multiteacher_soft_pseudo_fast_public_c4_20260414T1922Z` | Fast retry of MS36: same five-teacher committee and soft pseudo training, but builder uses source top-160, fused top-200, cluster rank top-200, and disables teacher-dropout stability to avoid the top-300 unique-candidate failure. | Completed on remote GPU0; builder produced `114129` pseudo rows / `3271` pseudo clusters, confidence mean `0.7689`; epoch 2 final loss `8.223330`, acc `0.851156`; public C4 validator passed with `top10_mean_score_mean=0.7735`, label_used_share `0.8894`, Gini@10 `0.3220`, max in-degree `42`; submission SHA-256 `2c16e42bea22bedfb241473d05dafdcefc89c3db7ab6f6de3ca3ac111fac4623`. | `0.6906` | `+0.6127` vs organizer baseline, `-0.0473` vs MS32, `-0.0112` vs MS31 | Rejected as a replacement for MS32. Multi-teacher soft pseudo improved local graph diagnostics dramatically but still damaged hidden-transfer geometry; local C4 is not reliable for late MS32 pseudo variants. |
| 2026-04-14 | `H9_official_eres_filtered_pseudo_public_c4_20260414T2057Z` | Official/pretrained ERes MS32-style staged run: H8 supervised low-LR adaptation from `iic/speech_eres2net_large_sv_zh-cn_3dspeaker_16k`, H8 public top-200 graph, MS32-style cluster-first pseudo pool filtered to `[8,80]`, then H9 filtered pseudo self-training and official ERes public C4 tail. | Failed at H8 train batch size `128` with CUDA OOM on GPU0 before any epoch completed; log `artifacts/logs/H9_official_eres_filtered_pseudo_public_c4_20260414T2057Z.log`. | not submitted | not applicable | Diagnostic launch failure. Superseded by batch-64 retry `H9_official_eres_filtered_pseudo_public_c4_20260414T2104Z`. |
| 2026-04-14 | `H9_official_eres_filtered_pseudo_public_c4_20260414T2104Z` | Batch-64 retry of the same official/pretrained ERes MS32-style staged run; train batch `64` with gradient accumulation `2`, H8/H9 tails keep inference batch `128`. | Manually stopped at H8 epoch `1/4`, batch `515/10310` (`5.0%`), loss `16.426107`, acc `0.000061`, `119.8` examples/s; GPU0 about `44369/81559 MiB`. | not submitted | not applicable | Diagnostic sizing run. Batch `64` was safe but under-used memory; superseded by batch-96 retry `H9_official_eres_filtered_pseudo_public_c4_20260414T2113Z`. |
| 2026-04-14 | `H9_official_eres_filtered_pseudo_public_c4_20260414T2113Z` | Batch-96 retry of the same official/pretrained ERes MS32-style staged run; train batch `96`, gradient accumulation `1`, H8/H9 tails keep inference batch `128`. | Completed on remote GPU0 end-to-end; H9 public C4 validator passed with `top10_mean_score_mean=0.6043`, `label_used_share=0.8668`, Gini@10 `0.2999`, max in-degree `50`; submission `artifacts/backbone_public/official_3dspeaker_eres2net_large_h9_pseudo_20260414T2113Z/submission.csv`, SHA-256 `720096b64ed24c9246e8e8404d422ead2606861c7ba04730710f1f92ce4c65cd`. | `0.5834` | `+0.5055` vs organizer baseline, `+0.0139` vs MS1, `-0.1184` vs MS31, `-0.1545` vs MS32, `-0.1639` vs MS41 | Rejected as a standalone replacement. Official ERes staged pseudo transfers above the unadapted MS1 baseline, but remains far below the CAM++ safe branches; keep only as an orthogonal family for future fusion diagnostics if needed. |
| 2026-04-14 | `MS37_campp_ms31_cnceleb_mixed_subcenter_lmft_20260414T2134Z` | MS31 clean-branch external adaptation: Stage A participant + CN-Celeb mixed supervised training with domain-balanced 70/30 batches, sub-center ArcMargin, small Inter-TopK penalty; Stage B one-epoch LMFT with 10s crops, margin `0.4`, no heavy augmentation; public tail remains MS32 C4. | Launched detached watcher on remote GPU1 with PID `499912`; waiting for CN-Celeb extraction while downloader is at about `7.69 GiB` / `37%`; old MS33 CN-Celeb watcher PID `482582` stopped to avoid GPU1 contention. | pending | pending | Active priority branch. FFSVC restricted audio remains unavailable without `ZENODO_ACCESS_TOKEN`, so this launch uses CN-Celeb as the available target-like external supervision and reserves FFSVC prefixes for later manifests. |
| 2026-04-15 | `MS38_campp_weight_soup_public_c4_20260415T0531Z` | Weight-space ensemble probe on the official CAM++ branch: interpolation line search between MS31 and MS32 plus MS30/MS31/MS32 weighted greedy-soup candidates, all evaluated as single checkpoints through the same packed-cache public C4 tail. | Completed on remote GPU1; all 6 validators passed. Submitted best local proxy `MS38a_i095` from `artifacts/submissions/MS38a_i095_campp_ms31_ms32_interp_20260415T0531Z_submission.csv`; local C4 `top10_mean_score_mean=0.6535`, overlap vs MS32 C4 `9.222/10`, top1 equal `92.86%`; validator passed, SHA-256 `68a03a885d69a720bc6cebc947ca2ab5001fc34f5eda183e4ada935b9b9da8e3`. | `0.7396` | `+0.6617` vs organizer baseline, `+0.0017` vs MS32, `-0.0077` vs MS41 | Accepted as a confirmed small gain over MS32 and evidence that near-MS32 weight interpolation can help hidden LB despite lower local proxy. Not current overall best if MS41 `0.7473` remains valid. |
| 2026-04-15 | `MS41_ms32_classaware_c4_weak_20260415T0530Z` | MS32 cached top-200 plus MS32 classifier posterior cache; H3b-style weak class-aware score bonus only inside cached neighbours (`overlap_top3=0.03`, same-top1 `0.01`, same-query-top3 `0.005`), then standard MS32 C4. No buckets and no class-first retrieval. | Validator passed; class cache computed from the MS32 checkpoint; pre-C4 adjusted top1 changed share `0.0836`; C4 `top10_mean_score_mean=0.6859`, label_used_share `0.9095`, Gini@10 `0.3437`, max in-degree `56`; overlap vs MS32 mean `8.63/10`, median `9`, top1 equal `87.76%`; submission `artifacts/submissions/MS41_ms32_classaware_c4_weak_20260415T0530Z_submission.csv`, SHA-256 `8b58013c3a710ef7e4c9f2fc5466ee9b2918d2ee271b5eaaa095b4976e194e84`. | `0.7473` | `+0.6694` vs organizer baseline, `+0.0094` vs MS32, `+0.0455` vs MS31 | New public best. Weak classifier posterior evidence transfers on top of MS32 when used only as a small edge bonus inside cached neighbours; accept MS41 as the current safe branch while keeping the lesson that hard class assignment remains unsafe. |
| 2026-04-15 | `MS40_rowwise_tail_router_20260415T0611Z` | Query-conditioned routing over existing public submissions: MS41 class-aware C4, MS32 full C4, MS38a_i095 soup C4, MS33b weak C4, MS33c reciprocal/local-only, and MS32 exact. Router uses MS32 C4 graph confidence, top1-top10 margin, reciprocal support, label size, same-label candidates, agreement/consensus across policies, classifier entropy, and duration when available. | Completed on remote GPU1; validator passed; selected policy shares: class-aware C4 `79.33%`, full C4 `7.04%`, weak C4 `6.22%`, exact `3.72%`, soup C4 `3.30%`, reciprocal-only `0.38%`; overlap vs MS41 mean `9.353/10`, top1 equal `93.68%`; overlap vs MS32 full C4 mean `8.962/10`; Gini@10 `0.3473`, max in-degree `108`; submission `artifacts/submissions/MS40_rowwise_tail_router_20260415T0611Z_submission.csv`, SHA-256 `b7171cffe374ccf232358a33f445a42fc22585d516796fde7c3abb32bbb4ace1`. | `0.7441` | `+0.6662` vs organizer baseline, `+0.0062` vs MS32, `-0.0032` vs MS41 | Rejected as a replacement for MS41. Query-conditioned routing beats MS32/MS38 but loses to the simpler MS41 class-aware C4 safe branch; keep MS40 as diagnostic evidence that local row-wise gates need stronger calibration before replacing the global policy. |
| 2026-04-15 | `MS39_campp_ms31_bn_adapter_cnceleb_20260415T0630Z` | Parameter-efficient CN-Celeb domain adaptation from MS31: freeze CAM++ encoder weights except BatchNorm affine tensors, allow BN running-stat updates, train a small cosine-proxy head on participant + CN-Celeb mixed manifest, then standard MS32 C4 tail. | Launched in combined MS39 watcher on remote GPU1, PID `511026`; log `artifacts/logs/MS39_bn_adapter_cnceleb_20260415T0630Z.log`; it only waited for `datasets/CN-Celeb_flac`, archive `13.84 GiB`. | not submitted | not applicable | Stopped/replaced on 2026-04-15 because CN-Celeb download was still at `66%` with about `9h30m` ETA. Switched to no-CN filtered-pseudo BN adapter run to avoid waiting. |
| 2026-04-15 | `MS39b_campp_ms32_bn_adapter_cnceleb_20260415T0630Z` | Same BN-affine CN-Celeb adaptation, but initialized from the MS32 filtered-pseudo checkpoint and using slightly lower LR/lighter augmentation than MS39. | Queued as the second branch in the stopped CN watcher; never reached training. | not submitted | not applicable | Stopped/replaced together with the CN watcher. |
| 2026-04-15 | `MS39_campp_ms31_bn_adapter_pseudo_20260415T0639Z` | No-CN fast PEFT branch: start from MS31, freeze CAM++ encoder except BN affine tensors and BN running stats, train one epoch on the existing MS32 filtered public pseudo mixed manifest, then standard official CAM++ C4 tail. | Completed on remote GPU1 in run group `MS39_no_cn_bn_adapter_pseudo_20260415T0639Z`; local C4 validator passed, `top10_mean_score_mean=0.6598`, `label_used_share=0.8406`, Gini@10 `0.3323`, max in-degree `81`; submission `artifacts/submissions/MS39_campp_ms31_bn_adapter_pseudo_20260415T0639Z_submission.csv`, SHA-256 `d22a53c83115e143da13200803feddab83880fa3ec60d4ca308c9f5e6d627848`. | `0.7031` | `+0.6252` vs organizer baseline, `+0.0013` vs MS31, `-0.0348` vs MS32, `-0.0442` vs MS41 | Rejected as a replacement for MS32/MS41. BN-only pseudo from MS31 barely improves over MS31 and stays materially below the current safe branches. |
| 2026-04-15 | `MS39b_campp_ms32_bn_adapter_pseudo_refresh_20260415T0639Z` | No-CN fast PEFT refresh from MS32: same filtered pseudo manifest, one epoch, restored MS32 classifier head because the manifest speaker index matches the checkpoint (`14021` speakers), then standard C4 tail. | Completed in the same GPU1 run group; local C4 validator passed, `top10_mean_score_mean=0.6536`, `label_used_share=0.8928`, Gini@10 `0.3330`, max in-degree `57`; submission `artifacts/submissions/MS39b_campp_ms32_bn_adapter_pseudo_refresh_20260415T0639Z_submission.csv`, SHA-256 `c671933363ad4f15277809386a9641ec508e72742d81a43816a8922b440940a9`. | not submitted | not applicable | Deprioritized without public submission. It finished with a weaker local proxy than MS39 branch A, which already underperformed on the public leaderboard. |
| 2026-04-16 | `W2V1k_stage1_teacher_peft_public_c4_20260416T144453Z_fast_bs1024_w4p1` | Public-tail metric run for the reusable stage1 teacher-PEFT checkpoint `artifacts/baselines/w2vbert2-mfa-lora-stage1/20260415T103816Z-b2c32e479771/teacher_peft` using the same fast extraction path and C4 tail profile as the winning W2V1j stage3 submission. | First launch failed immediately because `test_public_manifest.csv` still pointed to `<repo-root>/...` from another host; fixed `scripts/run_teacher_peft_c4_tail.py` with `--data-root` fallback, synced it to remote, and relaunched. Final local metrics: `embedding_s=2539.024440`, `search_s=0.643123`, `rerank_s=11.733930`; `top10_mean_score_mean=0.7499`, `top1_score_mean=0.8054`, `label_used_share=0.9126`, Gini@10 `0.2969`, max in-degree `41`; validator passed; local submission copy `artifacts/submissions/W2V1k_stage1_teacher_peft_public_c4_20260416T144453Z_fast_bs1024_w4p1_submission.csv`. | `0.8062` | `+0.7283` vs organizer baseline, `+0.0589` vs MS41, `-0.0282` vs W2V1j stage3 | Accepted as a strong stage-ablation result. Even stage1 already beats the previous CAM++ safe branch `MS41`, but the final stage3 checkpoint still holds a meaningful edge, so later fine-tuning stages are not cosmetic. |
| 2026-04-16 | `W2V1l_stage2_teacher_peft_public_c4_20260416T144453Z_fast_bs1024_w4p1` | Public-tail metric run for the completed stage2 teacher-PEFT checkpoint `artifacts/baselines/w2vbert2-mfa-joint-ft-stage2/20260415T175740Z-24273cd620a9/teacher_peft` using the same fast extraction path and C4 tail profile as the winning W2V1j stage3 submission. | Completed on remote GPU1; `embedding_s=2684.535089`, `search_s=0.700455`, `rerank_s=11.773768`; `top10_mean_score_mean=0.7444`, `top1_score_mean=0.8003`, `label_used_share=0.9161`, Gini@10 `0.3045`, max in-degree `42`; validator passed; remote submission `artifacts/backbone_public/teacher_peft/W2V1l_stage2_teacher_peft_public_c4_20260416T144453Z_fast_bs1024_w4p1/submission_W2V1l_stage2_teacher_peft_public_c4_20260416T144453Z_fast_bs1024_w4p1.csv`; local copy `artifacts/submissions/W2V1l_stage2_teacher_peft_public_c4_20260416T144453Z_fast_bs1024_w4p1_submission.csv`, SHA-256 `fce58c1006301d3dfafbb4b9eba2e1b6b2b7b412c57853625f58ae7f89f80d7e`. | `0.8222` | `+0.7443` vs organizer baseline, `+0.0749` vs MS41, `+0.0160` vs W2V1k stage1, `-0.0122` vs W2V1j stage3 | Accepted as the public stage2 ablation result. External public ordering is now cleanly `stage1 < stage2 < stage3`, which shows that joint full fine-tuning already recovers most of the final quality and that the local C4-tail proxy understated the real stage2 gain over stage1. |
| 2026-04-16 | `W2V1j_teacher_peft_public_c4_20260416T131620Z_fast_bs1024_w4p1` | Public-tail submission build from the completed W2V1j stage3 teacher-PEFT checkpoint. Rewrote extraction around worker-fed `DataLoader` batches, tuned queue geometry on `remote`, and launched the winning `batch=1024`, `num_workers=4`, `prefetch=1` profile on `GPU1`. | validator passed; `embedding_s=2448.668842`, `search_s=0.691275`, `rerank_s=11.364635`; `top10_mean_score_mean=0.7456`, `top1_score_mean=0.8008`, `label_used_share=0.9076`, Gini@10 `0.3264`, max in-degree `49`; submission copy `artifacts/submissions/W2V1j_teacher_peft_public_c4_20260416T131620Z_fast_bs1024_w4p1_submission.csv`, SHA-256 `688e10555ef307807c28dd454439d1ca2f477b67bda7a5b0869144d8f3c5853a`. | `0.8344` | `+0.7565` vs organizer baseline, `+0.0871` vs MS41 | New public best. The large-PTM teacher-PEFT branch decisively beats the previous safe CAM++ branch `MS41` and becomes the current top public candidate; keep the caveat that this is an external public observation and still validate private-overfit risk via shadow-public checks. |
| 2026-04-16 | `SPEED1_family_onnx_tensorrt_comparison_20260416` | Speed comparison for one strongest source per family: organizer baseline unchanged, CAM++ MS32 encoder feeding MS41 tail, W2V1j stage3 teacher-PEFT, and H9 official ERes2Net-large. Added generic ONNX/TensorRT tooling, TensorRT encoder backends for W2V/ERes, ERes parallel inference-only FBank workers, Python TensorRT layer profiler, suite collector, and SVG renderer. | Local checks passed (`ruff`, `ty`, targeted ONNX/TensorRT tests `10 passed`). Remote full-submit timings on `remote` `GPU1`: baseline `254.828s`, CAM++ TensorRT `80.486s`, W2V TensorRT `2562.068s`, ERes TensorRT `345.641s`; all generated CSVs validator-clean. Source overlap mean@10/top1: CAM++ `8.595`/`87.28%`, W2V `9.880`/`98.94%`, ERes `7.345`/`69.12%`. Layer profile JSONs and chart recorded under `artifacts/speed-family-comparison/` and `research/docs/assets/speed-comparison.svg`. | not submitted | not applicable | Completed diagnostic/serving workstream. CAM++ is the fastest prepared full-submit path; W2V remains the best public-LB source model but is frontend-bound and much slower. TensorRT CSVs are not score-equivalent unless separately submitted; public LB scores remain external observations from source submissions. |
| 2026-04-16 | `MS42_campp_ms32_tensorrt_public_c4_20260416` | Public leaderboard check of the SPEED1 CAM++ TensorRT artifact `artifacts/submissions/SPEED_CAMPP_MS32_TRT_PUBLIC_c4_submission.csv`, produced by MS32 TensorRT encoder plus MS41 class-aware C4 tail. | Local validator passed with `134697` rows, `k=10`, errors `0`; SHA-256 `632159c03085a200abf76511493dcbb53f359fb2421af30f3de14358b986ca30`; full-submit timing from SPEED1 `80.486s`; overlap vs source MS41 mean@10 `8.595`, top1 `87.28%`, row same-set `44.88%`. | `0.7381` | `+0.6602` vs organizer baseline, `-0.0092` vs MS41, `+0.0002` vs MS32 | Rejected as a score-preserving replacement for MS41. TensorRT speed path is valid and fast, but FP16/runtime differences change enough nearest-neighbour ordering to lose about `0.0092` public LB versus the source MS41 CSV. Keep for speed demo; use exact/reference source CSV when leaderboard score preservation is mandatory. |
| 2026-04-16 | `SPEED2_modelopt_maxopt5_overnight_20260416` | Follow-up speed/footprint run for all non-baseline families using NVIDIA ModelOpt ONNX AutoCast FP16 plus TensorRT `builder_optimization_level=5`. Added maxopt5 configs, ModelOpt wrapper, TensorRT builder-level plumbing, detailed TensorRT profiling, ONNX initializer-size accounting, and a `model_path` metadata fix for W2V/ERes bundles. | Local code gates passed (`ruff`, `ty`, targeted TensorRT unit test `5 passed`; after metadata fix, `ruff` and `ty` passed for `scripts/apply_modelopt_onnx_autocast.py`). Installed `nvidia-modelopt[onnx]` in the remote repo `.venv`. First launch failed before compute; retry1 completed CAM++ then stopped on W2V metadata; retry2 completed W2V+ERes. Full-submit timings on `remote` `GPU1`: CAM++ `86.408s`, W2V `2494.929s`, ERes `344.473s`; all generated CSVs validator-clean. Source overlap mean@10/top1: CAM++ `8.585`/`87.31%`, W2V `0.0006`/`0.00%`, ERes `7.345`/`69.13%`. W2V TensorRT build validation failed with `NaN` outputs despite CSV format validity. Layer profile engine footprints: CAM++ plan `21.710 MiB` / device memory `1012.8 MiB`; W2V plan `1139.170 MiB` / device memory about `47.0 GiB`; ERes plan `58.957 MiB` / device memory about `10.2 GiB`. | not submitted | not applicable | Completed diagnostic. CAM++ and ERes maxopt5 are valid speed artifacts but do not improve materially over SPEED1. W2V ModelOpt FP16 maxopt5 is rejected as score-unsafe because ranking overlap collapsed; do not use it for leaderboard or final delivery. |
| 2026-04-17 | `SPEED3_w2v1j_stage3_safe_speed_repair_20260417` | Repaired W2V speed path after the ModelOpt failure. Added `--embedding-cache-path` to `run_teacher_peft_c4_tail.py` so accepted W2V1j embeddings can skip audio decode, HF feature extraction, and encoder execution, then recompute exact top-k, C4 rerank, validator, and CSV write. Added `configs/release/w2v1j-stage3-safe-speed.toml`; CAM++ remains on SPEED1. | Local and remote `ruff`/`ty` passed for the updated W2V tail script. On `remote` `GPU1`, `SPEED3_W2V1J_STAGE3_SOURCE_EMB_CACHE_C4` used the accepted W2V1j embedding cache and completed in `12.077s` script wall (`14.251s` shell wall): embedding load `0.027s`, search `1.041s`, C4 rerank `9.928s`, write `0.576s`, validator `0.499s`. Validator passed. Overlap vs W2V1j source: mean@10 `10.0`, top1 `100%`, ordered-cell equality `100%`; SHA-256 exactly `688e10555ef307807c28dd454439d1ca2f477b67bda7a5b0869144d8f3c5853a`. Reference fast path also passed validator and materialized the byte-identical CSV in `0.907s`. | identical to W2V1j source `0.8344` by SHA | inherits W2V1j `+0.7565` vs organizer baseline | Accepted as the safe W2V fixed-public speed path. Use cached-embedding C4 when recomputing the tail is required; use reference fast path for exact fixed-public delivery. Do not use W2V ModelOpt maxopt5. |

## 2026-04-16 — W2V1h stage2 completed, wrapper crashed after report finalization

- Active run checked:
  - wrapper run id:
    `W2V1h_w2vbert2_mfa_lora_lmft_resume_s2_20260415T175719Z`
  - wrapper log:
    `artifacts/logs/W2V1h_w2vbert2_mfa_lora_lmft_resume_s2_20260415T175719Z.log`
- Completed stage2 artifacts:
  - stage2 local run id: `20260415T175740Z-24273cd620a9`
  - output root:
    `artifacts/baselines/w2vbert2-mfa-joint-ft-stage2/20260415T175740Z-24273cd620a9`
  - checkpoint:
    `artifacts/baselines/w2vbert2-mfa-joint-ft-stage2/20260415T175740Z-24273cd620a9/teacher_peft`
  - training summary:
    `artifacts/baselines/w2vbert2-mfa-joint-ft-stage2/20260415T175740Z-24273cd620a9/training_summary.json`
  - score summary:
    `artifacts/baselines/w2vbert2-mfa-joint-ft-stage2/20260415T175740Z-24273cd620a9/score_summary.json`
  - verification report:
    `artifacts/baselines/w2vbert2-mfa-joint-ft-stage2/20260415T175740Z-24273cd620a9/verification_eval_report.json`
  - markdown report:
    `artifacts/baselines/w2vbert2-mfa-joint-ft-stage2/20260415T175740Z-24273cd620a9/teacher_peft_report.md`
  - reproducibility snapshot:
    `artifacts/baselines/w2vbert2-mfa-joint-ft-stage2/20260415T175740Z-24273cd620a9/reproducibility_snapshot.json`
- Observed stage2 verification metrics from the completed run:
  - `score_gap=0.694516`
  - `eer=0.015162`
  - `min_dcf=0.102414`
  - scoring throughput after the chunked rewrite stabilized around `129k trials/s`
    and finished `90,754,128` trials in about `11m41s`
- Failure mode:
  - training, embedding export, trial generation, scoring, verification, reproducibility,
    and markdown report all completed successfully
  - the wrapper then crashed before stage3 handoff because the remote
    `src/kryptonite/training/teacher_peft/pipeline.py` was still on the pre-fix version
    that tried to access `verification_report.slice_dashboard_path`
  - traceback ended with:
    `AttributeError: 'WrittenVerificationEvaluationReport' object has no attribute 'slice_dashboard_path'`
- Decision:
  - do not rerun stage2
  - reuse the completed stage2 checkpoint and continue directly into stage3 after syncing
    the fixed runtime files to `remote`

## 2026-04-16 — W2V1i/W2V1j resume stage3 from completed stage2 checkpoint

- Local fixes applied before the next remote resume:
  - `scripts/run_w2vbert2_sv_moonshot.py`
    - added `--stage2-checkpoint` so the wrapper can reuse a completed stage2 run and
      continue directly to stage3 while still writing a combined summary JSON
  - `src/kryptonite/training/teacher_peft/model.py`
    - fixed PEFT checkpoint-format detection so merged backbones are written as
      `checkpoint_format="full_model"` even if the merged model still exposes a
      stale `peft_config` attribute
  - `tests/unit/test_teacher_peft_checkpoint.py`
    - added regression coverage for merged-backbone checkpoint writing
- Local validation:
  - `ruff check src/kryptonite/training/teacher_peft/model.py tests/unit/test_teacher_peft_checkpoint.py scripts/run_w2vbert2_sv_moonshot.py src/kryptonite/training/teacher_peft/pipeline.py`
    passed
  - `pytest tests/unit/test_teacher_peft_checkpoint.py` passed (`3 passed`)
  - `python scripts/run_w2vbert2_sv_moonshot.py --help` succeeded with the new
    `--stage2-checkpoint` argument
- Remote corrective sync:
  - synced updated files to `remote`:
    - `scripts/run_w2vbert2_sv_moonshot.py`
    - `src/kryptonite/training/teacher_peft/pipeline.py`
    - `src/kryptonite/training/teacher_peft/model.py`
- First stage3 resume attempt:
  - wrapper run id:
    `W2V1i_w2vbert2_mfa_lora_lmft_resume_s3_20260416T073258Z`
  - result: failed immediately during checkpoint load
  - diagnosis:
    the completed stage2 checkpoint contained full-model files under
    `teacher_peft/adapter/` (`config.json`, `model.safetensors`), but its
    `checkpoint_metadata.json` incorrectly declared
    `checkpoint_format = "peft_adapter"`
  - resulting error:
    `ValueError: Can't find 'adapter_config.json' at '.../teacher_peft/adapter'`
- Remote artifact repair for the already completed stage2 checkpoint:
  - updated
    `artifacts/baselines/w2vbert2-mfa-joint-ft-stage2/20260415T175740Z-24273cd620a9/teacher_peft/checkpoint_metadata.json`
    to set `checkpoint_format = "full_model"`
  - this matches the actual saved files and avoids retraining stage2
- Active replacement run:
  - wrapper run id:
    `W2V1j_w2vbert2_mfa_lora_lmft_resume_s3_fixfmt_20260416T073554Z`
  - log:
    `artifacts/logs/W2V1j_w2vbert2_mfa_lora_lmft_resume_s3_fixfmt_20260416T073554Z.log`
  - pid file:
    `artifacts/logs/W2V1j_w2vbert2_mfa_lora_lmft_resume_s3_fixfmt_20260416T073554Z.pid`
  - summary target:
    `artifacts/reports/w2vbert2/W2V1j_w2vbert2_mfa_lora_lmft_resume_s3_fixfmt_20260416T073554Z_summary.json`
  - stage3 local run id:
    `20260416T073610Z-787cea6b9872`
  - stage3 output root:
    `artifacts/baselines/w2vbert2-mfa-lmft-stage3/20260416T073610Z-787cea6b9872`
- Initial stage3 snapshot after the repaired resume:
  - wrapper logged successful reuse of both stage1 and stage2 checkpoints
  - stage3 entered training with:
    - `batch_size=48`
    - `eval_batch_size=16`
    - `epochs=1`
    - `train_batches=15921`
  - first live batch:
    - `epoch 1/1 batch 1/15921`
    - `loss=4.576860`
    - `avg_acc=0.937500`
    - `ex_per_sec=5.11`
    - `eta=41h31m47s` (cold-start estimate, expected to drop after warmup)
  - GPU1 snapshot:
    - `100%` utilization
    - `14531 MiB` allocated
    - about `294 W`
- Decision:
  - keep `W2V1j...` as the active run on `gpu1`
  - stage2 is accepted as complete and should not be rerun unless stage3 reveals a
    downstream incompatibility
