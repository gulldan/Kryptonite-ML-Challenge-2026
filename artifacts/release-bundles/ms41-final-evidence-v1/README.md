# Kryptonite MS41 final evidence bundle

Documentary release bundle for the final submitted candidate
`MS41_ms32_classaware_c4_weak_20260415T0530Z`.

This bundle is intended for jury/package handoff when the current local workspace does
not contain the full runtime artifact set. It stages the final written evidence, the
release configs, the closest locally available parent runtime records (`MS32`), and a
machine-readable record of the missing `MS41` runtime paths, SHA, and timestamps.

Entry points:

- `submission_bundle.md` -- human-readable summary
- `submission_bundle.json` -- machine-readable manifest
- `sources/runtime_external_references.json` -- recorded final `MS41` runtime paths
