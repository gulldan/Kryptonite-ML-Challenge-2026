# Tests

Test structure is split by scope:

- `tests/unit/` for isolated fast checks
- `tests/integration/` for multi-module integration checks
- `tests/e2e/` for top-level workflows and system tests
