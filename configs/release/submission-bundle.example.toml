title = "Kryptonite smoke submission bundle"
bundle_id = "demo-smoke-v1"
bundle_mode = "smoke"
release_tag = "demo-smoke-v1"
summary = "Self-contained smoke bundle for the current demo/runtime contract. Replace the placeholder paths with frozen release artifacts for a real candidate handoff."
output_root = "artifacts/release-bundles/demo-smoke-v1"
create_archive = true
require_tensorrt_plan = false

repository_readme_path = "README.md"
model_card_path = "docs/model-card.md"
runbook_path = "docs/release-runbook.md"
documentation_paths = [
  "docs/final-benchmark-pack.md",
  "docs/triton-deployment.md",
]
data_manifest_paths = [
  "artifacts/demo-subset/demo_subset.json",
]
code_fingerprint_paths = [
  "pyproject.toml",
  "uv.lock",
  "src",
  "apps",
  "scripts",
  "configs",
]
benchmark_paths = []
config_paths = [
  "configs/deployment/infer.toml",
  "configs/deployment/infer-gpu.toml",
]
checkpoint_paths = []
supporting_paths = [
  "artifacts/export-boundary-smoke/export_boundary.json",
  "artifacts/inference-stress/inputs/stress_input_catalog.json",
]

model_bundle_metadata_path = "artifacts/model-bundle/metadata.json"
onnx_model_path = "artifacts/model-bundle/model.onnx"
tensorrt_plan_path = ""
threshold_calibration_path = ""
demo_assets_root = "artifacts/demo-subset"
triton_repository_root = "artifacts/triton-model-repository"

notes = [
  "The checked-in artifacts are smoke/demo artifacts unless explicitly replaced by a frozen candidate bundle.",
  "Switch bundle_mode to candidate and add benchmark/threshold/checkpoint paths for a real release handoff.",
]
